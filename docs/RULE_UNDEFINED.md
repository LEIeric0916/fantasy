# RULE_UNDEFINED 清單（第一階段）

本清單只記錄來源未確認項，不給出答案。運行時卡表清單由 `getRuleUndefinedInventory()` 產生：所有 `null` 與所有非空 `notes` 都會轉成結構化 `RULE_UNDEFINED` 記錄；資料中的確認/更正備注也保留，供審計最後一次修正。

## 會阻斷第一階段流程的規則

目前沒有卡牌關鍵數值為 `null` 的阻斷項。齒輪費用為 1、機械士兵制造艙為 3，其余先前缺少費用的七張衍生牌費用均為 2。

## 核心規則待確認

雙方同時觸發時各自排列，並由當前回合玩家的整批效果先處理；兩批仍共享同一時機快照。

目前沒有核心流程層級的未確認規則。赤焰的龍皇兵與巴哈姆特轉費返回牌組後的處理已確認為洗牌。

## 卡牌 `notes` 中仍需確認的項目

- 龍族：當前上述逐卡問題均已由最後修正確認。
- 不朽者：當前上述逐卡問題均已由最後修正確認。
- 機械：當前上述逐卡問題均已由最後修正確認。
- 聯盟：當前上述逐卡問題均已由最後修正確認。聯盟主牌目前有38張，禁止自行補齊缺少的2張。

完整 77 條非空 `notes`（包括已確認的最後修正）不在本文件重復抄寫，以 JSON 原文及 `getRuleUndefinedInventory()` 輸出為準，避免清單與資料源漂移。
