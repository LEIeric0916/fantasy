# RULE_UNDEFINED 清单（第一阶段）

本清单只记录来源未确认项，不给出答案。运行时卡表清单由 `getRuleUndefinedInventory()` 产生：所有 `null` 与所有非空 `notes` 都会转成结构化 `RULE_UNDEFINED` 记录；资料中的确认/更正备注也保留，供审计最后一次修正。

## 会阻断第一阶段流程的规则

目前没有卡牌关键数值为 `null` 的阻断项。齿轮费用为 1、机械士兵制造舱为 3，其余先前缺少费用的七张衍生牌费用均为 2。

## 核心规则待确认

双方同时触发时各自排列，并由当前回合玩家的整批效果先处理；两批仍共享同一时机快照。

目前没有核心流程层级的未确认规则。赤焰的龙皇兵与巴哈姆特转费返回牌组后的处理已确认为洗牌。

## 卡牌 `notes` 中仍需确认的项目

- 龙族：当前上述逐卡问题均已由最后修正确认。
- 不朽者：当前上述逐卡问题均已由最后修正确认。
- 机械：当前上述逐卡问题均已由最后修正确认。
- 联盟：当前上述逐卡问题均已由最后修正确认。联盟主牌仍只有35张，禁止补齐缺少的5张。

完整 77 条非空 `notes`（包括已确认的最后修正）不在本文件重复抄写，以 JSON 原文及 `getRuleUndefinedInventory()` 输出为准，避免清单与资料源漂移。
